$(window).load(function() {
	$("#TCPIP").click(function() {
		var screenWidth = screen.width;
		var screenHeight = screen.height;

		alert(screen.width);
		alert(screen.height);
	});
}(jQuery));